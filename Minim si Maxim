#include <iostream>

using namespace std;

int main ()
{
    int x, maxi, mini, n;
    cin>>n;
    cin>>x;
    maxi=mini=x
    for(int i=2; i<=n; i++)
    {
        cin>>x;
        if(x>maxi) maxi=x;
        if(x<mini) mini=x;
    }
    cout<<maxi<<" "<<mini;
    return 0;
}
